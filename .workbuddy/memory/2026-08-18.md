# 2026-08-18 工作记录 · 明朝知识抽取项目

## 状态：全 7 部抽取 100% 完成 ✅

### 抽取进度
- 启动后台 general-purpose 抽取 agent（max_turns 950），补齐剩余 61 章（p5/p6/p7 全部）。
- 完成后核验：`data/extract_raw.json` 共 **156 条**，分部 p1:31 / p2:22 / p3:20 / p4:22 / p5:20 / p6:20 / p7:21，目标 61 章 0 缺失。
- `src/merge.py` 重新生成 `data/data.json`（约 1.3MB）：角色 1252 / 地点 640（已标坐标 136）/ 事件 1106 / 关系 1402 / 时间轴 1106。

### 流水线（确认可用，无需改）
- `src/_dump_chapter.py <key>` 导出某章正文（避免加载 4.5MB chapters.json）。
- `src/_append_extract.py <rec.json>` 按 key 去重追加/覆盖进 extract_raw.json。
- 抽取记录 schema：`{key, part, chapter, time, characters[], locations[], events[], relations[]}`（字符/地点/事件/关系四元组 + 时间）。
- 重启安全：agent 每次先读 extract_raw.json 收集已有 key，跳过已抽取项。

### 报告：新增全 7 部版本（不破坏现有 Part-1 报告）
- 复制 `.dump/gen_report2.py` → `.dump/gen_report_all.py`，将硬编码的 `p1set`（仅壹部 31 章）改为 `allset = set(raw.keys())`（全部 156 章抽取 key）。
- 相应替换 `for k in P1`→`ALL`、所有 `p1set`→`allset`；输出路径改为 `web/report_full.html`；标题/统计/页脚改为「七部全景报告 / 七部 156 章 / 全七部（洪武→崇祯）」。
- 生成结果：`web/report_full.html`（约 976KB，数据全内联）：地点 136（有坐标）、角色卡 1249、时间线 1071、章节打卡 156。
- **保留 `web/report.html`（壹部 31 章版本，用户此前表示很满意）未被改动。**
- 后续待用户决定：保留双报告 / 把 full 提升为主 / 给 full 加「按部/按势力」筛选（1249 张卡全量 DOM 偏重，可加搜索框改善可用性）。

### 数据质量小注
- 王阳明仅出现 1 次，疑似抽取多用本名「王守仁」；属别名覆盖细节，非阻断。
- p5–p7 多数地点尚无 `geo_annotations` 坐标（仅 136 条为 p1–p4 时期标注），故「按地域/地图」视图仍为这 136 个有坐标点；「按章节」视图会列全部地点（今址可能为空）。后续可补 p5–p7 坐标。

### 遗留中间文件
- `.dump/body_*.txt`（62 个，约 2.6MB）为抽取期各章正文转储，属中间产物；未删除，待用户确认是否清理。

---

## 更新：用户决策「② 提升 full 为主 + ③ 加筛选」已落地 ✅（~12:23）

- **② 提升为主报告**：`cp web/report.html web/report_p1.html`（壹部 234KB 备份）→ 重跑 `gen_report_all.py` → `cp web/report_full.html web/report.html`。
  - 现 `web/report.html` = 全 7 部 156 章 + 筛选版（980KB）；`web/report_p1.html` = 壹部 31 章（留底）；`web/report_full.html` 与 `report.html` 字节一致（均为 full+筛选，属冗余但可保留为生成目标）。
- **③ 角色卡筛选**：在 `gen_report_all.py` 中加 HTML `filterbar` + CSS + JS：
  - 「按部」7 选 1（p1–p7 部名）；「按势力」input+`datalist`（474 势力去重排序，精确匹配）；「搜索」框（姓名/身份/事件/关系 模糊）；「重置」按钮。
  - 实现：`partsOf(c)` 从章节 key 前缀取部；`applyFilter()` 三重过滤后调 `renderChars(list)` 重渲屏幕卡 + 打印正/背面（按筛选结果），并显示「显示 N / 总数」。
  - 切换筛选后自动回到「屏幕翻转卡」视图，避免停在打印态。
- 校验：`report.html` 含 `filterbar:8 / applyFilter:5 / 按势力 / 按部` 等标识，确认筛选代码已随生成入库。
- 注意：早前若干 502 报错是 harness 回传经过 copilot.tencent.com 代理失败所致，本地文件操作实际已成功；以文件实际内容为准。
