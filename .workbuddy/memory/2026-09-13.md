# 2026-09-13 工作日志

## 主线：按《Ming_全面重构方案》落地（report/Ming_全面重构方案.txt）

来源：用户指定该文档「按这个优化下」。先做代码勘验（逐条核对方案中的论断是否属实），再做 Phase 1 + Phase 2。

### 勘验结论（方案属实项，均有实测证据）
- 关系图 741 个端点里 694 是人物、**47 是非人物**（东林党/东厂/内阁/后金/北京/明朝/黄河/朝鲜…）→ 文案却写「741 名人物」。属实。
- 孤立人物口径错：起点是「人物表 1231 − 图节点 741 = 490」，正确应为 1231 − 686 = **545**。属实。
- 构建耗时实测 **109.7 秒**，瓶颈是 `build_relation_graph_full` 里 numpy 的 800 轮 O(n²) FR 布局。属实。
- numpy 缺失时静默降级成圆环布局并返回 `links: []`（构建"成功"但图是坏的）。属实（P1-04 silent failure）。
- `audit_final.py`：`[9]` 规则是 `for ...: pass` 死代码；`not l.get('lat')` 真值判断；未知年份用 `not e.get('year')`（报告用数值解析）→ 审计 6 vs 报告 7；**无论发现什么一律 exit 0**。全部属实。
- `extract_auto.py` 解析了 `--dense` 却没传（`call_model(title, body)`）。属实。
- `sw.js` 命中缓存时直接 `return cached`，后台更新未 `waitUntil` 保活。属实。
- data.json 地点 561、报告 581（event_places 注入）→ 审计与产物不同源。属实。

### 已完成改动
1. `src/extract_auto.py`：`call_model(title, body, dense=dense)`。
2. 新增 `src/core/__init__.py`、`src/core/year_parser.py`（year_bounds / has_numeric_year）、`src/core/geo.py`（is_valid_lat/lng、has_coords、coord_problem）。`generate_report.py` 的 `year_bounds` 改为薄包装，审计共用。
3. `src/generate_report.py`：
   - `build_relation_graph_full` 整段重写（`.dump/_patch_graph.py` 行区间替换，带"区间内只有一个顶层 def"断言）：只保留两端都在人物表内的关系；degree/isolated 在人物集合内统计；统计字段扩为 `{nodes,edges,persons,connected,isolated,excludedNonPerson}`；坐标改为**确定性势力分扇区+螺旋**（O(n)、无随机、无 numpy）。
   - 新增 JS `fullSummaryHTML(g)` 单点生成统计文案（renderFullGraph / resetFullHighlight 共用）。
   - `build_scope` 新增 `scope_names` 与 `relation_scope`（full=all，分部=induced），payload 增加 `relationScope`，关系视图顶部注明策略。
   - `_fgAbort` 提升为模块级（原挂 canvas 会泄漏 window 监听）；`loadLeaflet` 失败时 `_leafletPromise=null`。
   - `import math` 补上（原用 `__import__("math")`）。
4. `src/audit_final.py` 整篇重写：`import generate_report as G` 直接审 `G.build_scope("full")` 最终模型；分级 INFO/WARNING/ERROR；ERROR → `sys.exit(1)`；真 stale 规则；逐部跑 p1~p7 验关系诱导子图；`--quick` 可跳过分部。
5. `sw.js`：`event.waitUntil(network)` 保活 + `CACHE` bump 到 `v4`。
6. 文档：README（构建 2 秒、审计新口径、关系图口径、待办新增「同名异地待拆」「两处待决策冲突项」「Phase 3~6 待排期」）；`.workbuddy/skills/ming-report-engineering/SKILL.md`（新增「共享核心」「全面重构落地」「前端验证（无浏览器时）」三节 + 故障表补 6 行 + 删掉重复的「确认 GitHub 与本地差异」整节与重复表行）。

### 实测结果
- 构建 **109.7s → 2.3s**；报告规模不变：156 章 / 1231 人 / 1106 事件 / 2324 关系 / 581 地点（定位 540）。
- 新关系图：**686 人 / 1403 边 / 孤立 545（44.3%）/ 排除 72 条含非人物端点的关系**，非人物节点 0，悬空边 0，坐标落在画布内。
- 审计：INFO 16 / WARNING 2 / ERROR 0 → 通过。WARNING 为「7 件未知年份事件」（与页面一致）与「延安府同名异地」（真问题）。
- p1~p7 关系诱导子图全部干净（p1 225 人/328 条 … p7 215 人/431 条）。

### 新发现的真问题（已记入 README 待办）
- **延安府同名异地**：`geo_annotations.json` 里有两条——陕西延安（36.59,109.49）与**朝鲜黄海南道延安**（37.95,126.55，万历援朝战场，黑田长政被赵宪民兵击败处）；地点表只取了陕西那条。需拆成两条，否则朝鲜巡礼点会错落到陕西。
- geo 标注 93 条用旧称（应天→南京、濠州→凤阳、平江→苏州…）已被 `mentioned_as` 别名覆盖，**非脏数据**（审计以 INFO 列出）。判定 stale 必须"先查原名再查别名"，否则虚报 93 条——这条规则我第一版写错了，第二版才修对。

### 环境坑（本会话新发现，已写入 skill）
- Bash 的 PATH 被裁：`ls` / `tail` / `dirname` / `agent-browser` 全部 command not found，但 `cd`/`echo`/绝对路径 exe 可用。对策：外部程序全用绝对路径（python/node），列目录改用 python `os.listdir`。
- `agent-browser` 找不到 → 前端验证改用 **Node 抽 DATA + eval 渲染函数 + 断言不变量**（比截图更硬）。
- 编辑工具偶发「报成功但文件未落盘」：本次 `state.charQuery` 筛选取代第一次没生效（占位符那次生效了），重发才写入。**改完必须 grep 复核**。

### 待办（需用户决策，未动）
- **别名搜索 vs 姓名字段搜索**：方案 P2-01 要崇祯→朱由检；用户此前明确要求「只搜名字字段，无匹配即空态」。冲突，待定。
- **地图预加载**：方案 P2-06 建议不预载；此前用户明确要求后台预热消除等待。冲突，待定。
- Phase 3~6（canonical id / 拆分 generate_report / pytest+Actions / deep link）未启动。
- 部署：本轮已完成部署（见文末「部署终态」）。

## 事故：原书全文曾公开躺在 GitHub 仓库（发现并处置）
- **发现**：本地↔线上 blob sha 全量比对时，比对工具报「仅线上：data/chapters.json」。核实：线上 4,497,814 B / 168 章含 body，**与本地份 sha 完全一致**（1300c4099a1984e2dd77ba4cbb330d3d3921fe05）。
- **根因**：.gitignore 里写了 data/chapters.json，但该文件早在 2026-09-04 的「Add files via upload」（commit 7190215d）就混进仓库——.gitignore 只挡 git add，挡不住 Contents/Git Database API 上传。此后所有同步脚本都「有意排除」它，所以既没被覆盖、也没被发现。
- **处置**：.dump/_remove_book_text.py 用 Git Database API 把该 path 置 sha:null 建新 tree → 新 commit → PATCH refs（commit 470918be），已从 main 移除并验证；本地文件不受影响。
- **遗留**：历史 commit 7190215d 仍可访问该 blob（GitHub 按 sha 可直达，直到 GC）。彻底清除需**重写历史**（孤儿 commit + force refs）或删库重建 / 找 GitHub Support —— **待用户决定**。
- **顺带**：把线上独有的 .gitignore、requirements.txt 取回本地（本地此前缺失）；线上 data/chapters_index.json 仅章节元信息（无正文），保留无妨。
- **加固**：skill 新增「合规巡检」节（部署后必跑一条命令确认线上无禁书产物）；比对脚本 .dump/_diff_remote.py 输出「仅线上」即为告警。

## 部署终态
- 部署 commit 82705f9（index.html+sw.js+README，线上已验证 sw v4 / stats 686·1403·545·72）。
- 同步 commit cd25c6a（src/core + audit + skill/memory）→ 移除禁书 commit 470918be → 终态 cb60918。
- 本地 108 文件 / 线上 54 文件；**过时 0**；未传仅为 .dump/ 一次性辅助脚本与已弃用的 .dump/git-sync/ 镜像（54 个，含 4MB index.html，冗余待清理）。


## 第二轮（同日）：Phase 3~6 全量落地
- **Phase 3 数据模型**：`entity_id()` → `person:朱由检`；`relation_id()` = `relation:<sha1[:12]>`（内容寻址、重跑稳定）；characters 增 `id/type/factions/factionRaw`，relations 增 `id/sourceId/targetId/sourceType/targetType`；payload 增 `model:{schemaVersion:2,entityTypes,idIndex(2918)}`；`parse_factions()` 把「东林」并入「东林党」。
- **Phase 4 拆分**：`HTML_TEMPLATE`（125,103 字符）拆到 `web/template/index.html` + `web/css/app.css` + `web/js/app.js`；`generate_report.py` 170,728 → 46,430 字符（1606 → 1155 行）。**验收：拼回与原文逐字节相同**（`equal=True`）。踩坑：骨架锚点后多一个换行 → 差 2 字符，去掉后归零；`__DATA__`/`__INSIGHT_DATA__` 随脚本落在 app.js，不在骨架（查占位符要查 JS）。
- **新增 `src/build.py`**：`--scope/--target standalone|web/--out/--check/--json`。standalone 直接调 `generate_report.compose_document()`（注入逻辑只保留一份），web target 输出 `dist/<scope>/index.html + assets/{app.css,app.js,data.js}`。产物与旧 `generate_report.py` **sha256 完全相同**（ac5254fb…）。
- **Phase 5 校验/测试/CI**：新增 `src/validators.py`（10 组不变量，ERROR/WARNING/INFO）作为唯一真源，被 build --check / audit_final（只吸收 ERROR）/ tests / CI 共用；`tests/` 28 例（零依赖 runner + pytest 兼容）：core / data_invariants / template。`.github/workflows/ci.yml`：版权巡检→语法→单测→构建门禁→深审→构建产物+无头浏览器自检。**CI 首次失败**（`.dump/_browser_check.py` 没进同步清单），补进 FILES + `Path.resolve()` + root 下 `--no-sandbox` 后 **green（run 34749970430）**。
- **无头浏览器自检（新手段，替代 agent-browser）**：`.dump/_browser_check.py` 用本机 Chrome `--headless=new --dump-dom` 跑 7 场景（首屏/人物/事件/帝王 deep link/搜索高亮/关系/地图）。关键教训：产物把 JS 内联在同文件里，**断言必须用正则匹配真实标签**，裸串 `'flash' in dom` 会假通过；`flash` 类 2.6s 自摘 → 该用例单独 `budget=1500`。
- **Phase 6 体验**：URL deep link（`view/person/detail/event/place/era/map/q`，README 有表），`person=` 支持别名并滚动定位+闪烁；搜索命中高亮（`MutationObserver` 看 `main`，`mark.hl`，`_hlSuppress` 时间窗防自触发死循环）；tabs `role=tablist/tab/tabpanel` + 方向键；打开详情把实体写回地址栏（`history.replaceState`）。顺手把两处重复的人物详情模板收敛成 `personDetailHTML()/showPerson()`（`.dump/_refactor_person_detail.py`）。**未做**：人物图/实体图双模式（与方案 A 冲突，需先定口径）、移动端抽屉（现有 dialog 已自适应）。
- **决策落地**：用户明确「别名搜索、地图预加载都要」→ 两者均已实现且不冲突（别名走 `aliasIndex` 444 条 + `matchesQuery()`；地图保留首屏后台预热并叠加 hover/touchstart 即时预热）。
- **SW** bump `v5`（Phase 3~6 大版本，bump 后首访 cache miss → 立刻拿新版）。
- **部署终态（第二轮）**：`_deploy_index_now.py` → commit e1cbd30（index.html 5,240,990 B 与本地字节一致、sw v5）；`_sync_docs.py` → 2625e53 → CI 修复 e6e1a5d。线上 71→72 文件，**过时 0**，合规巡检无 `chapters.json`/`明朝那些事儿`。两个部署脚本补了 3 次重试与 `MING_DEPLOY_MESSAGE`/`MING_SYNC_MESSAGE` 覆盖。
- **未决（延续）**：历史 commit 7190215d 里的 `data/chapters.json` blob 仍可访问，是否重写历史待用户决定。
