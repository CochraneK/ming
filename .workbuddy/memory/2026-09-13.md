# 2026-09-13 工作日志

## 主线：按《Ming_全面重构方案》落地（report/Ming_全面重构方案.txt）

来源：用户指定该文档「按这个优化下」。先做代码勘验（逐条核对方案中的论断是否属实），再做 Phase 1 + Phase 2。

### 勘验结论（方案属实项，均有实测证据）
- 关系图 741 个端点里 694 是人物、**47 是非人物**（东林党/东厂/内阁/后金/北京/明朝/黄河/朝鲜…）→ 文案却写「741 名人物」。属实。
- 孤立人物口径错：起点是「人物表 1231 − 图节点 741 = 490」，正确应为 1231 − 686 = **545**。属实。
- 构建耗时实测 **109.7 秒**，瓶颈是 `build_relation_graph_full` 里 numpy 的 800 轮 O(n²) FR 布局。属实。
- numpy 缺失时静默降级成圆环布局并返回 `links: []`（构建"成功"但图是坏的）。属实（P1-04 silent failure）。
- `audit_final.py`：`[9]` 规则是 `for ...: pass` 死代码；`not l.get('lat')` 真值判断；未知年份用 `not e.get('year')`（报告用数值解析）→ 审计 6 vs 报告 7；**无论发现什么一律 exit 0**。全部属实。
- `extract_auto.py` 解析了 `--dense` 却没传（`call_model(title, body)`）。属实。
- `sw.js` 命中缓存时直接 `return cached`，后台更新未 `waitUntil` 保活。属实。
- data.json 地点 561、报告 581（event_places 注入）→ 审计与产物不同源。属实。

### 已完成改动
1. `src/extract_auto.py`：`call_model(title, body, dense=dense)`。
2. 新增 `src/core/__init__.py`、`src/core/year_parser.py`（year_bounds / has_numeric_year）、`src/core/geo.py`（is_valid_lat/lng、has_coords、coord_problem）。`generate_report.py` 的 `year_bounds` 改为薄包装，审计共用。
3. `src/generate_report.py`：
   - `build_relation_graph_full` 整段重写（`.dump/_patch_graph.py` 行区间替换，带"区间内只有一个顶层 def"断言）：只保留两端都在人物表内的关系；degree/isolated 在人物集合内统计；统计字段扩为 `{nodes,edges,persons,connected,isolated,excludedNonPerson}`；坐标改为**确定性势力分扇区+螺旋**（O(n)、无随机、无 numpy）。
   - 新增 JS `fullSummaryHTML(g)` 单点生成统计文案（renderFullGraph / resetFullHighlight 共用）。
   - `build_scope` 新增 `scope_names` 与 `relation_scope`（full=all，分部=induced），payload 增加 `relationScope`，关系视图顶部注明策略。
   - `_fgAbort` 提升为模块级（原挂 canvas 会泄漏 window 监听）；`loadLeaflet` 失败时 `_leafletPromise=null`。
   - `import math` 补上（原用 `__import__("math")`）。
4. `src/audit_final.py` 整篇重写：`import generate_report as G` 直接审 `G.build_scope("full")` 最终模型；分级 INFO/WARNING/ERROR；ERROR → `sys.exit(1)`；真 stale 规则；逐部跑 p1~p7 验关系诱导子图；`--quick` 可跳过分部。
5. `sw.js`：`event.waitUntil(network)` 保活 + `CACHE` bump 到 `v4`。
6. 文档：README（构建 2 秒、审计新口径、关系图口径、待办新增「同名异地待拆」「两处待决策冲突项」「Phase 3~6 待排期」）；`.workbuddy/skills/ming-report-engineering/SKILL.md`（新增「共享核心」「全面重构落地」「前端验证（无浏览器时）」三节 + 故障表补 6 行 + 删掉重复的「确认 GitHub 与本地差异」整节与重复表行）。

### 实测结果
- 构建 **109.7s → 2.3s**；报告规模不变：156 章 / 1231 人 / 1106 事件 / 2324 关系 / 581 地点（定位 540）。
- 新关系图：**686 人 / 1403 边 / 孤立 545（44.3%）/ 排除 72 条含非人物端点的关系**，非人物节点 0，悬空边 0，坐标落在画布内。
- 审计：INFO 16 / WARNING 2 / ERROR 0 → 通过。WARNING 为「7 件未知年份事件」（与页面一致）与「延安府同名异地」（真问题）。
- p1~p7 关系诱导子图全部干净（p1 225 人/328 条 … p7 215 人/431 条）。

### 新发现的真问题（已记入 README 待办）
- **延安府同名异地**：`geo_annotations.json` 里有两条——陕西延安（36.59,109.49）与**朝鲜黄海南道延安**（37.95,126.55，万历援朝战场，黑田长政被赵宪民兵击败处）；地点表只取了陕西那条。需拆成两条，否则朝鲜巡礼点会错落到陕西。
- geo 标注 93 条用旧称（应天→南京、濠州→凤阳、平江→苏州…）已被 `mentioned_as` 别名覆盖，**非脏数据**（审计以 INFO 列出）。判定 stale 必须"先查原名再查别名"，否则虚报 93 条——这条规则我第一版写错了，第二版才修对。

### 环境坑（本会话新发现，已写入 skill）
- Bash 的 PATH 被裁：`ls` / `tail` / `dirname` / `agent-browser` 全部 command not found，但 `cd`/`echo`/绝对路径 exe 可用。对策：外部程序全用绝对路径（python/node），列目录改用 python `os.listdir`。
- `agent-browser` 找不到 → 前端验证改用 **Node 抽 DATA + eval 渲染函数 + 断言不变量**（比截图更硬）。
- 编辑工具偶发「报成功但文件未落盘」：本次 `state.charQuery` 筛选取代第一次没生效（占位符那次生效了），重发才写入。**改完必须 grep 复核**。

### 待办（需用户决策，未动）
- **别名搜索 vs 姓名字段搜索**：方案 P2-01 要崇祯→朱由检；用户此前明确要求「只搜名字字段，无匹配即空态」。冲突，待定。
- **地图预加载**：方案 P2-06 建议不预载；此前用户明确要求后台预热消除等待。冲突，待定。
- Phase 3~6（canonical id / 拆分 generate_report / pytest+Actions / deep link）未启动。
- 部署：本轮已完成部署（见文末「部署终态」）。

## 事故：原书全文曾公开躺在 GitHub 仓库（发现并处置）
- **发现**：本地↔线上 blob sha 全量比对时，比对工具报「仅线上：data/chapters.json」。核实：线上 4,497,814 B / 168 章含 body，**与本地份 sha 完全一致**（1300c4099a1984e2dd77ba4cbb330d3d3921fe05）。
- **根因**：.gitignore 里写了 data/chapters.json，但该文件早在 2026-09-04 的「Add files via upload」（commit 7190215d）就混进仓库——.gitignore 只挡 git add，挡不住 Contents/Git Database API 上传。此后所有同步脚本都「有意排除」它，所以既没被覆盖、也没被发现。
- **处置**：.dump/_remove_book_text.py 用 Git Database API 把该 path 置 sha:null 建新 tree → 新 commit → PATCH refs（commit 470918be），已从 main 移除并验证；本地文件不受影响。
- **遗留**：历史 commit 7190215d 仍可访问该 blob（GitHub 按 sha 可直达，直到 GC）。彻底清除需**重写历史**（孤儿 commit + force refs）或删库重建 / 找 GitHub Support —— **待用户决定**。
- **顺带**：把线上独有的 .gitignore、requirements.txt 取回本地（本地此前缺失）；线上 data/chapters_index.json 仅章节元信息（无正文），保留无妨。
- **加固**：skill 新增「合规巡检」节（部署后必跑一条命令确认线上无禁书产物）；比对脚本 .dump/_diff_remote.py 输出「仅线上」即为告警。

## 部署终态
- 部署 commit 82705f9（index.html+sw.js+README，线上已验证 sw v4 / stats 686·1403·545·72）。
- 同步 commit cd25c6a（src/core + audit + skill/memory）→ 移除禁书 commit 470918be → 终态 cb60918。
- 本地 108 文件 / 线上 54 文件；**过时 0**；未传仅为 .dump/ 一次性辅助脚本与已弃用的 .dump/git-sync/ 镜像（54 个，含 4MB index.html，冗余待清理）。


## 第二轮（同日）：Phase 3~6 全量落地
- **Phase 3 数据模型**：`entity_id()` → `person:朱由检`；`relation_id()` = `relation:<sha1[:12]>`（内容寻址、重跑稳定）；characters 增 `id/type/factions/factionRaw`，relations 增 `id/sourceId/targetId/sourceType/targetType`；payload 增 `model:{schemaVersion:2,entityTypes,idIndex(2918)}`；`parse_factions()` 把「东林」并入「东林党」。
- **Phase 4 拆分**：`HTML_TEMPLATE`（125,103 字符）拆到 `web/template/index.html` + `web/css/app.css` + `web/js/app.js`；`generate_report.py` 170,728 → 46,430 字符（1606 → 1155 行）。**验收：拼回与原文逐字节相同**（`equal=True`）。踩坑：骨架锚点后多一个换行 → 差 2 字符，去掉后归零；`__DATA__`/`__INSIGHT_DATA__` 随脚本落在 app.js，不在骨架（查占位符要查 JS）。
- **新增 `src/build.py`**：`--scope/--target standalone|web/--out/--check/--json`。standalone 直接调 `generate_report.compose_document()`（注入逻辑只保留一份），web target 输出 `dist/<scope>/index.html + assets/{app.css,app.js,data.js}`。产物与旧 `generate_report.py` **sha256 完全相同**（ac5254fb…）。
- **Phase 5 校验/测试/CI**：新增 `src/validators.py`（10 组不变量，ERROR/WARNING/INFO）作为唯一真源，被 build --check / audit_final（只吸收 ERROR）/ tests / CI 共用；`tests/` 28 例（零依赖 runner + pytest 兼容）：core / data_invariants / template。`.github/workflows/ci.yml`：版权巡检→语法→单测→构建门禁→深审→构建产物+无头浏览器自检。**CI 首次失败**（`.dump/_browser_check.py` 没进同步清单），补进 FILES + `Path.resolve()` + root 下 `--no-sandbox` 后 **green（run 34749970430）**。
- **无头浏览器自检（新手段，替代 agent-browser）**：`.dump/_browser_check.py` 用本机 Chrome `--headless=new --dump-dom` 跑 7 场景（首屏/人物/事件/帝王 deep link/搜索高亮/关系/地图）。关键教训：产物把 JS 内联在同文件里，**断言必须用正则匹配真实标签**，裸串 `'flash' in dom` 会假通过；`flash` 类 2.6s 自摘 → 该用例单独 `budget=1500`。
- **Phase 6 体验**：URL deep link（`view/person/detail/event/place/era/map/q`，README 有表），`person=` 支持别名并滚动定位+闪烁；搜索命中高亮（`MutationObserver` 看 `main`，`mark.hl`，`_hlSuppress` 时间窗防自触发死循环）；tabs `role=tablist/tab/tabpanel` + 方向键；打开详情把实体写回地址栏（`history.replaceState`）。顺手把两处重复的人物详情模板收敛成 `personDetailHTML()/showPerson()`（`.dump/_refactor_person_detail.py`）。**未做**：人物图/实体图双模式（与方案 A 冲突，需先定口径）、移动端抽屉（现有 dialog 已自适应）。
- **决策落地**：用户明确「别名搜索、地图预加载都要」→ 两者均已实现且不冲突（别名走 `aliasIndex` 444 条 + `matchesQuery()`；地图保留首屏后台预热并叠加 hover/touchstart 即时预热）。
- **SW** bump `v5`（Phase 3~6 大版本，bump 后首访 cache miss → 立刻拿新版）。
- **部署终态（第二轮）**：`_deploy_index_now.py` → commit e1cbd30（index.html 5,240,990 B 与本地字节一致、sw v5）；`_sync_docs.py` → 2625e53 → CI 修复 e6e1a5d。线上 71→72 文件，**过时 0**，合规巡检无 `chapters.json`/`明朝那些事儿`。两个部署脚本补了 3 次重试与 `MING_DEPLOY_MESSAGE`/`MING_SYNC_MESSAGE` 覆盖。
- **未决（延续）**：历史 commit 7190215d 里的 `data/chapters.json` blob 仍可访问，是否重写历史待用户决定。
- **第三轮（同日）：方案逐条验收**。用户明确「`data/chapters.json` 不用管了」（放弃重写 git 历史），并要求汇报重构完成度。逐条核对 `report/Ming_全面重构方案.txt` 的 25 条（P1×12 / P2×10 / P3×3）→ 产出 **`report/Ming_重构验收清单.md`**：✅17 / 🟡6 / ❌1 / ⏸1（有意不做）。
  - **实跑复核（本次）**：`build --scope full` **2.68s**，产物 5,240,990 B；`build --check` ERROR 0 / WARNING 1 / INFO 3；`audit_final --quick` INFO 10 / WARNING 2 / ERROR 0；`tests` **28/28（3.8s）**；仓库 HEAD `7a3cf13` CI **green**；线上 index.html 与本地**逐字节一致**（5,240,990 B）；合规巡检 0 命中。
  - **图口径实值**：`relationGraphFull.stats = {nodes 686, edges 1403, persons 1231, connected 686, isolated 545, excludedNonPerson 72}`；`characters[]` 字段 = `faction`(数组)/`factions`/`factionRaw`(473 个原始串)/`id`/`aliases`…，**无** `office`/`dynasty`/`origin`/`jinshi_year`；`relations[]` 带 `sourceId`/`targetId`/`endpointKind` 但 `from`/`to` 仍为显示名；`aliasIndex` 444；`unknownTimeline` 7（与审计 7 一致，P1-05 已闭环）。
  - **剩余 5 项**（均可选）：① P2-03 faction 深度结构化（拆 office/籍贯/科举，前端去 `cleanCardFields` 正则）；② P3-03 统一错误 UI（现仅地图一处降级提示）；③ Phase 6 双模式图（需先定口径）；④ P2-10 补 `#view=timeline&from=&to=`；⑤ P3-01 体积压缩（单文件仍 5.0 MB）。
  - **P2-08 残留**：`.dump/_sync_docs.py`、`.dump/_deploy_index_now.py` 仍有 `D:/2026/...` 硬编码（本机内部脚本，未改）。**P2-09 有意不做**（`.workbuddy/memory` 作换机接续指南保留在仓库）。

- **第四轮（同日）：把剩余 5 项全部补齐**（用户：「继续完成没做的」）。终态 **✅23 / 🟡1 / ⏸1**，方案条目全部闭环。
  - **P2-03 势力结构化**：新建 `src/core/faction_profile.py`（唯一真源）。`parse_profile(faction_raw, role, reign_years)` → 12 字段；覆盖率 政权 1161/1231（94.3%）、官职 426、身份类别 320、派系 109、籍贯 26、科举 16、未归类 16。要点：① `label` 优先级必须是 派系>身份类别>机构>官职>政权（`_OFFICE_RE` 会吞掉前面的派系名）；② 解析 role 必须用**完整原串**——`clean_role()` 截断会让李三才的 label 从「东林党」退成「凤阳巡抚」，故加 `item["_roleRaw"]` 专供解析、解析后 pop 掉不进 payload；③ 派系在 role 里只用严格档 `factions_from_role()`（只认自我认同句式），否则「被东林借杨镐事攻击」会被算成东林党人；④ 科举正则兼顾「万历三十五年1607进士」这种四位年夹在中间的写法（两侧都要 `(?<!\d)`/`(?!\d)`）；⑤ 籍贯在括号内放宽到 2~4 字府县（宁波/无锡/山阴），但必须挡身份词尾 `_ORIGIN_STOP_CHARS`（门/伙/友/家/商/学/旧/系…），覆盖 22→26 且 0 误判（10 个非籍贯形态全部正确拒绝）。前端 `cleanCardFields()` 改为**优先读 profile**，详情页输出结构化 chips + 「原串：…」回查行。新增校验 `V-PROFILE-01~06`。
  - **Phase 6 双模式图**：新建 `src/core/graph_layout.py`（两图共享确定性布局 + `GRAPH_KIND_LABELS`）；`build_relation_graph_entities()` 用「真人优先」判类型；payload 加 `relationGraphEntities`，schemaVersion 2→3。**实体图 741 实体 / 1467 条**（人物 694 + 机构 12 + 其他 16 + 地点 15 + 政权 4），与方案原始数字吻合；人物图口径未变（686/1403）。前端 `activeGraph()` 为模式唯一入口（11 处替换）、类型图例 `.kind-dot`、按模式分别写统计/提示/邻域文案、`#netMode` 加第三项 + 口径说明。子集契约（1403 ⊆ 1467）有测试守。
  - **P2-10 时间轴区间**：`timelineRange()` + `#timelineFrom/#timelineTo/#timelineClear`，区间写回地址栏（`from`/`to`），deep link 与 tabs 清理同步；实测 `from=1400&to=1450` → 命中 166/1099。
  - **P3-03 统一错误 UI**：`failBar()/dismissFail()/reportRuntimeError()` + `.fail-*` 样式（三级配色 + 重试 + 关闭 + `role=alert`/`aria-live` + 打印隐藏），接入 5 类失败；**启动守卫独立成一段 script 并排在主脚本之前**（5s 未置位 `window.__MING_READY` 就渲染错误条）；删掉所有静默空 catch（测试断言该写法不存在）；后台预热改 `makeTiles(true)` 静默，避免用户没打开地图就被提示打扰。
  - **P3-01 体积压缩（含一个真实事故）**：为守卫脚本写注释时提到了占位符名，**字符串替换把整份 payload 注入进注释** → 数据被注入两遍，5.6MB → **11.4MB**。修掉后 + 注入改紧凑 JSON（`separators=(",", ":")`）→ **11.39MB → 5.13MB（−55%）**，gzip 1.60 → 0.83MB。防腐层：`test_data_placeholders_appear_exactly_once` + `test_compose_document_injects_payload_once`。另核实 `characters[].relations` **不是** `DATA.relations` 的纯投影（168/1231 人卡上更少，是有意取子集），**不能**改客户端推导去省那 854KB。
  - **P2-08 清尾**：`_sync_docs.py`（`BASE = Path(__file__).resolve().parents[1]`）、`_deploy_index_now.py`（`ROOT` 推导）硬编码清除，全部 `.dump/*.py` 已无 `D:/2026`。
  - **P3-02 无障碍补口**：`#fullGraph` canvas 加 `role="img"` + `aria-describedby="fullSummary"` + **按模式动态生成**的 `aria-label`；热力图与中心人物网络 SVG 各加 `role="img"` + 描述性 label。
  - **自查发现的另两个真 bug**：① `const activeGraph=()=>cond?A:activeGraph()` 无限自递归（栈溢出打挂整个图谱视图）→ 兜底必须回落到 `DATA.relationGraphFull`；② `#fullNet` 的 display 表达式漏了 `entity`，深链 `net=entity` 进来总图不显示（且 `#egoNet` 原本没有任何显示控制，full 模式两块同显）→ 补全模式分支。
  - **门禁实测**：`build --check` **ERROR 0 / WARNING 1 / INFO 5**；`audit_final` **INFO 17 / WARNING 2 / ERROR 0**；`tests` **45/45（4.0s）**；无头浏览器 **11/11 + 启动守卫**（新增实体图/人物图/时间轴区间/无障碍断言）；单文件 5.13MB。
  - **工程教训（已写进 skill）**：`--dump-dom` 把布尔属性序列化成 `key=""`（`selected=""`）；属性顺序不可假设；`no` 断言要区分「渲染出来」与「内联源码里写着」，为此给自检加了 `scoped`（锚定元素 + 限定窗口）能力；兜底路径用「注入语法错误的副本」验证。

- **第五轮（同日）：收尾确认闭环**（承接第四轮部署后的终态复核）。
  - **CI 全绿**：commit `8409716b`（Docs sync）+ 同名 `pages build and deployment` 均 **success**；`16f69c99`（Phase 4+5+6）两条 run 亦 **success**。
  - **部署终态**：`_deploy_index_now.py` → commit **`16f69c99`**（index.html 5,384,119 B / sw.js 2,460 B / README.md 28,693 B）；`_sync_docs.py` → commit **`8409716b`**（线上 73 → **76** 文件；新增 `src/core/faction_profile.py`、`src/core/graph_layout.py`、`report/Ming_重构验收清单.md`）。
  - **字节一致核验（本地 blob sha == 线上 blob sha）**：index.html `f27a3653b5` / sw.js `4e9b353bc0` / README.md `64ae8a20a2` / faction_profile.py `2123174a17` / graph_layout.py `35c7bc5b48` / 验收清单 `92bb724268` —— **6/6 全 OK**。
  - **差异检查** `.dump/_diff_remote.py`：**未传 0 / 过时 0 → 完全一致 ✅**。
  - **合规巡检** `gh api /repos/CochraneK/ming/git/trees/main?recursive=1` 过滤 `chapters\.json|明朝那些事儿` → **0 命中**；线上 `data/` 16 个文件全为派生数据，其中 `data/chapters_index.json`（40,388 B，仅分部/章标题/键名，无正文）是 2026-08-26 起**有意发布**的脱敏替代品，`_diff_remote.py` 已白名单放行，**不是事故**（勿再当遗留项排查）。
  - **清理**：删除两个一次性补丁脚本 `.dump/_fix_skill_dup.py`（914 B，删 SKILL.md 重复章节）、`.dump/_patch_graph.py`（6,921 B，替换 `build_relation_graph_full` 函数体）；二者均未进同步清单，删除后差异检查仍「完全一致」。
  - **结论**：重构方案 25 条 → **✅23 / 🟡1（P2-02 部分完成，有说明）/ ⏸1（P2-09 有意不做）**；Phase 3~6 全部落地；CI green 且线上与本地逐字节一致，本轮收拢完毕。

- **第六轮（同日）：转内容层——同名异地拆分机制 + 事件补年 + 地点定位**（用户：「继续」，工程层已闭环，接 `MEMORY.md` 的「未完成工作清单（内容层）」）。
  - **两级同名异地拆分机制（新，`src/merge.py`）**。此前只有「按原始串」一级；本轮补齐**按章节**一级，两级判据越靠前越优先：
    ① `location_splits_by_chapter = {章节key: {片段: 新名}}`；② `location_splits = {原始地点串: {片段: 新名}}`；③ `LOCATION_CANON` 兜底。
    **核心教训：判据必须落在「章节 / 原始串」上——只按片段名判无效，因为两处同名地点的片段名本来就相同。** `MANUAL_CORR` 路径提为常量；两份 splits 必须在地点归一化循环**之前**加载（其余勘误块仍在关系构建前应用）。
    - **修掉 2 处真实同名异地**：
      ① `延安府`——`p5-c13` 的原始串是 `延安府/清州`，其「延安府」是**朝鲜黄海道延安**（1592 壬辰倭乱黑田长政攻城三日不下，守将李廷馣；原 `mentioned_as` 把主将误写成赵宪，赵宪打的是同章清州）；`p7-c14` 的「延安府」是**陕西延安**（张献忠籍贯）。**原书自己就标注了「延安府（朝鲜地名）」，抽取时被丢掉**。坐标 126.0833/37.9167（GeoNames 1866571 与 Wikidata 一致）。
      ② `龙山`——`p5-c17` 是**朝鲜汉城近郊日军粮仓「龙山大仓」**（李如松令查大受夜袭焚毁，距汉城约 5 公里），`p4-c17` 是**浙江慈溪东南**（戚继光龙山之战）。两处原始串都是「龙山」，所以**必须走按章节那一级**。
  - **事件补年：7 件 → 2 件待考**。
    - `manual_event_years.json` 补 4 件（只回填 `year` 为空的事件）：空印案与郭桓案（提及）`"1376-1385"`、王振净身入宫教太子 `"1424-1427"`（approx）、杨博智退蛮番救翟銮 `1539`、陆炳平反诏狱冤狱 `1539`。
    - `manual_corrections.event_years` 补 1 件（**覆盖式**）：张居正掘坟之议引发三党裂痕 `1620`（approx）——因为它原 `year` 是「万历末年」**非空**，`manual_event_years` 的 `if e.get("year") in ("", None)` 不生效，这是本轮踩到的机制坑。
    - **发现 `year` 支持区间串**：`normalize_year()` 只把纯四位数字转 int，`"1376-1385"` 原样保留，再由 `year_bounds()` 解出 `year_start/year_end`（时间轴按起年排序 + 渲染区间）。所以补年可以直接写区间。
    - **保留待考 2 件**：严世蕃论天下三人、朱棣生母碽妃之谜——原书与史料均无可靠纪年（前者是纯对话场景，后者是后世身世考据）。按「宁可留空不可猜错」不动。
    - 考据依据：陆炳那条**原书 p4-c8 自己写了「嘉靖十八年（1539）」**；杨博那条经明史《列傳第一百二》＋ WebSearch 双证（嘉靖十八年随翟銮巡九边至肃州，属番数百遮道邀赏）。
  - **地点定位：41 → 30 个未定位**（补 12 个：月漾桥→雄安雄县赵北口、娄桑→涿州楼桑、乳峰山→锦州南松山主峰、笊篱山→葫芦岛塔山、南安→江西大余、龙山（朝鲜）→首尔龙山、龙山→浙江慈溪、镇江堡→辽宁丹东、五里寨→山西应县、六堡→辽宁宽甸宽奠六堡、登莱→山东蓬莱、暖阁→紫禁城乾清宫）。坐标取自现代行政区中心的一律如实标 `status:"现代行政区中心（待核验）"`。
    - 剩余 30 个多为**漠北蒙古草原地名**（口温、别笃山口、彻彻儿山、兀良哈秃城、大伯颜山、回曲津、康哈里海、沙珲原、齐拉尔河、黑松林、也失八秃儿、百眼井…），15 世纪蒙古地名现代定位困难且有争议，按纪律不动。
    - `车迟国` 给 `status:"非实地"` 且**不给坐标**——它是《西游记》虚构国名，原书借指嘉靖崇道灭佛，不是明代实地，不应绘入圣地地图。
    - 规模：地点 561 → **563**（延安 +1、龙山 +1），已标坐标 521 → **533**。
  - **门禁**：`build --check` **ERROR 0 / WARNING 1（待考事件 2 件，原 7）/ INFO 5**；`audit_final` **INFO 17 / WARNING 2 / ERROR 0**；`tests` **45/45**。
  - **skill 升级 v3.1.0 → v3.2.0**：merge 数据块清单补 `location_splits*`、`manual_event_years`、`geo_annotations` 三条说明；故障速查表新增「同名地点被并成一条」「补年写了却不生效」两行。
  - **收尾（同日）**：
    - **本地重建产物**：`python src/build.py` → `index.html` **5,390,004 B**（5.1 MB，1.5s）。抽查：`延安府（朝鲜）`/`龙山（朝鲜）` 各 5 处、`车迟国` 5 处、朝鲜延安坐标 `126.0833` 2 处、`龙山大仓` 1 处均入产物；`doc.count('"scopeLabel"')==1`（体积铁律自检通过）。
    - **前端真机自检** `.dump/_browser_check.py` **11/11 通过**（首屏 / 人物 / 事件 / 年号 / 搜索高亮 / 关系 / 地图懒加载 / 实体图 / 人物图 / 时间轴区间 / 启动守卫）。
    - **清理**：删除 `.dump/data.json.bak_before_split`（1,983,771 B，拆分前旧 `data.json` 快照，561 地点，可由 build 随时重建）——它是 diff 里唯一的 `[未传]` 项，删除后归零。
    - **差异检查** `_diff_remote.py`：`[未传] 0`；`[过时] 9`（本轮内容：`index.html`、`data/{data,geo_annotations,manual_corrections,manual_event_years}.json`、`src/merge.py`、`.workbuddy/{memory/2026-09-13.md,memory/MEMORY.md,skills/ming-report-engineering/SKILL.md}`）；`[仅线上] 1`（`data/chapters_index.json`，白名单伪遗留）。**→ 本轮内容已全部就位，等用户一句话即可 `_deploy_index_now.py` + `_sync_docs.py` 发布，本轮按记录未推送。**
  - **状态更正**：当日 21:30 用户回「同步」，本轮**已发布**（见下节）。

## 发布（同日 21:30~21:45，用户「同步」）

- **第一次 `_sync_docs.py`**：上传恰为那 9 个变化文件（`index.html` 5,390,004 B / `src/merge.py` / `data/{data,geo_annotations,manual_corrections,manual_event_years}.json` / `.workbuddy/{memory/2026-09-13.md,memory/MEMORY.md,skills/ming-report-engineering/SKILL.md}`），跳过 66 个 → commit **`87c45e83`**。
  - **未跑 `_deploy_index_now.py`**：它的三个文件（index.html / sw.js / README.md）本就包含在 `_sync_docs.py` 的清单里且已随本次上传，再跑只会多产生一个内容相同的空提交。**结论：`_sync_docs.py` 是超集，可单独完成发布。**
- **发现并修掉一个真实体验缺陷（自查，非用户报告）**：`sw.js` 的 `CACHE` 仍是 **`v6`**。SW 是 stale-while-revalidate —— 缓存名不变时，老用户首次打开拿到的仍是旧缓存，要**第二次刷新**才见新数据；而本轮恰恰是纯数据勘误（地点/年份），用户最在意的就是"打开即新版"。→ bump **`v6` → `v7`**（顺带在注释里记明 v7 内容），这会让浏览器检测到 sw.js 字节变化 → 新 SW 安装 → activate 清空旧缓存 → 立即拿到新数据。
  - 连带同步三处文档里的过期缓存名：`MEMORY.md`、`skills/ming-report-engineering/SKILL.md`（并在 SKILL 里补一条纪律「**纯数据勘误轮也要 bump**」，因为缓存名写错正是"内容没更新"排查的第一坑）、`report/Ming_重构验收清单.md` 的 P1-12 行。
  - 改完复跑 `tests` **45/45**（无测试硬编码 v6，安全）。
- **第二次 `_sync_docs.py`**：上传 `sw.js`(2762 B) + 上述 3 个文档，跳过 71 → commit **`07dd4ebe`**。
- **线上核验**：
  - `_diff_remote.py` → **完全一致 ✅**（未传 0 / 过时 0 / 仅线上 1 白名单）。
  - 合规巡检 → 76 个 blob，**敏感路径 0 命中**。
  - `https://cochranek.github.io/ming/index.html` 实拉：`延安府（朝鲜）`5 / `龙山（朝鲜）`5 / `车迟国`5 / `龙山大仓`1 / 朝鲜延安坐标 `126.0833` 2 —— 新内容**已在线上**。
  - `sw.js` 首次直拉只 1861 B 且无 v6/v7：**CDN 边缘缓存滞后**（Pages `max-age=600`）；加 `?v=1` 穿透后 Pages 与 `raw.githubusercontent.com` 均返回 **2762 B / `CACHE = CACHE_PREFIX + 'v7'`**。浏览器 SW 更新检查本身绕过 HTTP 缓存，故不影响实际更新。
  - Actions：`87c45e83` 与 `07dd4ebe` 的 **CI** 与 **pages build and deployment** 全部 **success**。
- **当前线上终态**：地点 563（已标坐标 533）/ 事件 1106（待考 2）/ 人物 1231 / 关系 2324；`CACHE ming-report-v7`。内容层待办只剩**人物卡语录扩面**（13/1231，候选池 71）与**洞察实体联动**（未动）。
