# 2026-08-28 项目审查与修复

## 用户请求
读取、熟悉、挑刺、完善、整理这个项目

## 审查范围
- 全部源码（11 个 .py，2778 行）
- 全部数据（9 个 JSON：data/extract_raw.json 156 章 + data/data.json 1.7MB + geo 685 条 + manual 942 条等）
- 报告产物（web/report.html 3.6MB + report_p1.html 0.9MB）
- 项目结构（src/ data/ web/ _publish/ _archived/ .dump/_archive/）

## 发现的问题与修复

### 已修复（commit 1c8075c）
1. **manual_relations.json 10 条手工关系丢失 bug**：`chapter=""`（空字符串）被 merge.py 的 `.get("chapter","curated")` 原样保留为 `""`，随后被 generate_report.py 的范围过滤（`!= "curated"` 且 `!= "推导"`）静默丢弃。涉及黄子澄↔齐泰、徐达↔常遇春、张玉↔朱能、铁铉↔盛庸、方孝孺↔黄子澄等 10 条重要建文/开国关系。修复：`chapter = mr.get("chapter") or "curated"`。
2. **enrich_geo.py GAZ 字典 5 个重复 key**：煤山/江陵/余姚/庐山/云南永昌各出现两次（Python dict 后者覆盖前者，无报错但数据不一致）。清理掉 3 条重复条目。
3. **README.md 链接过期**：`_publish/README.md` 在线演示链接仍用旧仓库名 `mingchao-knowledge-graph`（已于 2026-08-26 改名 `ming`），且路径多 `/web/`。修正为 `https://CochraneK.github.io/ming/`。

### 未修复（需用户决策或本机执行）
1. **_publish/src/ 与 src/ 的 generate_report.py、extract_auto.py 不同步**：`_publish/src/` 是旧快照，generate_report.py 差异大（113KB vs 99KB）。_publish/ 整体是发布用镜像，已部分同步（data/report/README），但 src/ 未更新。
2. **_publish/data/data.json 滞后**：旧版 locations=629 / relations=1399（当前 561/2331），已同步。
3. **geo_annotations.json 有 139 条 stale 条目**：geo 有 685 条，data.json 只用 561 个 ancient 名（差 124 条是历史遗留/已合并/已删地点）。
4. **data.json 有 16 个地点未在 geo_annotations 中**：普陀山/新郑/武陵/衡山/琼山/代州/黄山/嵩山/终南山/蕲州等（enrich_geo.py 的 GAZ 未覆盖）。
5. **_archived/ 目录（~1.6MB）**：旧 index_related 相关（echarts.min.js 1MB + china.json 580KB + index.html 28KB），已无用但未删。
6. **.dump/_archive/（47 个旧脚本 + .b64 临时文件）**：历史迭代归档，约 10MB，含 _check.js（3.6MB）、_fg_body.json（1.2MB）等大文件。
7. **src/__pycache__/clean_rules.cpython-313.pyc**：编译缓存，不应在项目里。
8. **尾端 8 章重抽**：仍需用户在装有 OmniRoute 的主机执行（沙箱无法连接 localhost:20128）。

## 部署
- 8 个文件字节级部署到 CochraneK/ming（HEAD a7c71e2）
- web/report.html 3666706B / data.json 1725609B / geo 117765B 等
- 全部 MATCH（README 远程 3876 = _publish/README.md 部署，本地简版 2678 不部署）

## 第二轮清理与补全（用户授权"都可以听你的"）
- **8 座名山坐标严重错误修复**：普陀山/衡山/嵩山/华山/太湖/黄山/雁荡山/九华山/终南山/泰山 之前全部被指向紫金山坐标 (118.85, 32.06)——因为 enrich_geo.py 的 GAZ 有一条超长复合地名条目 `"紫金山、太湖、普陀山、雁荡山..."` 被 merge.py 通过 `mentioned_as` 回退匹配，导致 9 个地点坐标全错。拆分为各自独立正确坐标。
- **geo stale 清理**：geo 685 条 → 655 条（清理 45 条不在 data.json 中且不作为别名目标的 stale 条目，保留 93 条作为 mentioned_as 别名目标）。
- **16 个缺漏地点补全**：data.json 16 个地点未在 geo 中有独立条目 → 全部补入 GAZ，缺漏 16 → 0。
- **_archived/ 删除**：旧 index_related（echarts.min.js 1MB + china.json 580KB），~1.6MB。
- **.dump/_archive/ 删除**：94 个旧迭代脚本 + .b64 临时文件，~14MB。
- **src/__pycache__/ 已不存在**（之前某轮已清）。
- **定位率**：349 → 350（+1），62.2% → 62.4%。
- **项目体积释放**：~15.6MB（_archived 1.6MB + .dump/_archive 14MB）。

## 第三轮：报告移到仓库根目录（用户要求"只希望一个 ming 后缀"，不要冗余）
- **根因**：原 `index.html` 用 `<meta http-equiv="refresh" content="0; url=web/report.html">` 客户端跳转，URL 变成 `/ming/web/report.html`。
- **改动**：`generate_report.py` 的 `OUT_PATH` 从 `BASE/"web"` 改为 `BASE`，全书报告输出 `index.html`（根），壹部版输出 `report_p1.html`（根）。删除本地 `web/` 目录与 `_publish/web/`。
- **README 更新**：`README.md` 与 `_publish/README.md` 链接改为根路径（`https://CochraneK.github.io/ming/`、`/report_p1.html`），仓库结构去掉 web/ 段。
- **部署**（HEAD f718d1e）：新增根 index.html / report_p1.html / src/generate_report.py，删除远程 `web/report.html` / `web/report_p1.html` / `web/index.html`（Git Database tree 中 sha=null = 删除；注意不能重复删已删路径，否则 BadObjectState 422）。
- **最终远程结构**：`.gitignore README.md data/ index.html report_p1.html requirements.txt src/`（无 web/ 目录），访问 `/ming/` 直接是完整报告，零跳转、URL 全程不变。
- **教训**：Git Database API 删文件用 tree 条目 sha=null；同一路径不可在同一 commit 重复删；删除要在 base_tree 对应 HEAD 上操作。

## 项目数据口径快照（2026-08-28 终态）
- 156 章 / 7 部 / 1235 人物 / 561 地点（350 已定位 62.4%）/ 1106 事件 / 2331 关系
- geo 655 条（655 条覆盖 561 个 ancient + 93 别名目标）
- manual_relations 942 条（10 curated + 932 推导，chapter="" bug 已修）
- 关系类别：君臣从属 396 / 其他 932（含推导）/ 敌对冲突 321 / 政治攻讦 193 / 亲属 188 / 同盟友党 162 / 地点关联 72 / 师生同门 63 / 婚姻姻亲 4

## 第四轮：最终一致性核验（用户：继续完成未完成的任务）
- 本地结构干净：根目录仅 `index.html`(3.6MB) + `report_p1.html`(0.9MB)，`web/` 已删，无 junk。
- 本地根产物与 `_publish/` 同步（index.html / report_p1.html / src 三件套 / data 三件套均 SAME）。
- 报告内链无失效 `web/` 引用（index.html 内无 `web/report_p1` href）；serve_report.bat 仅起 http.server，无路径依赖。
- **唯一剩余未完成任务：尾端 8 章重抽**，再次探测 OmniRoute 网关 `localhost:20128` → `WinError 10061 连接被拒`，沙箱仍不可达。需用户在装有 OmniRoute 的主机执行：
  `python src/extract_auto.py --dense --force p1-c17 p1-c22 p1-c28 p4-c11 p6-c12 p5-c2 p4-c4 p4-c15` → `python src/merge.py` → `python src/generate_report.py --scope full` → `.dump/_deploy_reports.py` 增量发布。
- 项目在沙箱内已无可执行的待办；所有可修复/可清理/可重构项均已完成并部署（HEAD f718d1e）。

## 第五轮：report_p1.html 删除 + HTML 严格自检与改进（用户："行，删除吧" / "自我严格检查html的布局、存在问题，进而改进"）
- **删除 report_p1.html**：本地根 + `_publish/report_p1.html` 删除；`_publish/src/ _publish/data/ _publish/index.html` 等旧镜像清理（仅 `_publish/README.md` 仍用于部署）；`generate_report.py` 改为 `--scope full` 只产出 `index.html`，保留 `--scope pN` 拆分能力（参数 choices 扩到 p1~p7）。部署（HEAD f843fdb）删远程 report_p1.html；线上 `/report_p1.html` 现 404，根 `/ming/` 仅 `index.html` 单一入口。
- **HTML 自检方法**：通读 `generate_report.py` 的 HTML_TEMPLATE（CSS + JS 渲染函数），并用 Python 抽取 `<style>`/`<script>` 逐条核对；再用 `node --check` 校验重生的内联 JS 语法。
- **发现并修复的布局/功能问题**：
  1. **[功能·高危] 翻面卡失效**：`renderCharacters` 每次渲染都 `document.addEventListener('click', …[data-flip]…)`，导致交互一次后监听叠加，翻面被 toggle 两次相互抵消→"翻面"按钮失效。改为顶部全局委托，只绑定一次。
  2. **[布局·中] 详情弹窗溢出视口**：`.dialog-body` 无 `max-height`/`overflow`，长详情（人物/事件）在移动端与桌面不可滚动。加 `#detailDialog{max-width:min(900px,94vw);max-height:90vh;padding:0;border:0}` + `.dialog-body{max-height:90vh;overflow:auto}`。
  3. **[响应式·中] 窄屏双栏挤压**：`.summary-hero`、`.distribution-intro` 无移动端断点，<760px 仍两列挤压。补 `@media(max-width:760px)` 折叠为单列并收紧 panel 内边距。
  4. **[细节·低] 力导图悬浮提示不换行**：`.full-graph-tip-box{white-space:nowrap}` 与 `max-width:240px` 冲突→长提示溢出，改为 `white-space:normal`。
- **已确认非问题的项**：`.toolbar`/`.character-toolbar` 已有 `flex-wrap`；`.table-wrap{overflow:auto}` + 表 `min-width:760px` 表格可横向滚动；`@media print` 打印样式已存在；离线地图 SVG 兜底完整；Leaflet 阻塞脚本在 head 是设计必须（renderMap 依赖 window.L，不可改 defer）；品牌 "明朝那些事儿 · 七部全书" 无重复。
- **部署**（HEAD 10a0f72）：根 index.html(3581KB) + src/generate_report.py(133KB) + README.md 增量更新。
- **复核教训（与第三轮一致）**：Git Database 删文件后，DELETES 列表必须清空，否则下一轮再列已删路径 → `BadObjectState 422`。本轮第二次部署即踩中，已清 DELETES 重跑成功。
- **未做（需用户决策）**：全图力导向动画在非 reduced-motion 下 alpha 触底 0.05 后仍持续 rAF（"微抖动"刻意效果，但常驻 CPU）；本次未改，保持布局改动范围最小。如要省电可在空闲 N 秒后停 rAF、交互时重启。

## 第六轮：补充/提取信息（用户："有没有可以进一步补充、提取的信息？确认todo，然后执行"）
- **缺口实测**：地点定位率 62.4%（350/561）；未知年份事件 35/1106（3.2%）；人物寿命 72 条（顶层 lifespans，已 join 70，属设计范围非缺失）；尾端8章重抽需本机 LLM 网关（沙箱仍 WinError 10061 不可达）。
- **Todo 三件**：① 未知年份事件补年（带源标注）；② 尾端8章重抽（本机准备，host-only）；③ 少量可信地点坐标补全。
- **① 事件补年（已执行·部署 HEAD 2f54b10）**：
  - 新增 `data/manual_event_years.json`（29 条可考事件：陈瑄漕运1415/巡抚制1430/朱高煦封云南1404/瓦剌吞并鞑靼1434/也先统一蒙古1439/土木堡相关1449-1450/王守仁系列1487-1508/唐伯虎1499/豹房1517/严嵩1542/徐海1555/李默1550/严嵩跪徐阶1562…），每条 `source:"记忆"` + `approx` 标记 + `note`。
  - `merge.py` 新增 `MANUAL_YEARS` 加载 + 聚合后按事件名回填 `year`，写入 `year_source/year_approx/year_note`，**不覆盖抽取已有年份**（与 manual_relations 同款解耦模式，重跑不丢）。
  - `generate_report.py` 的 `showEvent` 在时间块追加 `来源：记忆` / `约·` 徽标 + 备注，落实源透明度（新增 `.src-tag`/`.src-note` CSS）。
  - 未知年份事件 **35 → 6**（剩 6 条为谜案/复合/确无单一年份：空印案与郭桓案提及、朱棣生母碽妃之谜、王振净身入宫、杨博救翟銮、陆炳平反、严世蕃论三人），如实保留。
- **③ 地点坐标补全（已执行·部署 HEAD 27ab7a5）**：
  - `enrich_geo.py` GAZ 新增 ~100 条高置信可考点：江河（淮河/长江/辽河/钱塘江/湘江/岷江/金沙江/澜沧江…）、区域（燕云十六州/两淮/浙西/北直隶/北元…）、名迹（朱仙镇/太庙/武当/峨眉山/清江浦/喜峰口/野狐岭…）、**万历朝鲜战争朝日战场**（碧蹄馆/闲山岛/鸣梁海峡/开城/大同江/稷山/晋州/玉浦/唐浦/泗川/庆州/日本贱岳/长筱/关原…）。模糊漠北军镇（全宁/岭北/称海/亦集乃路…）留待专门核验，不强行补。
  - 全部命中即标 `坐标来自内置地名词典（待核验）`（脚本既有模式），与项目源透明度纪律一致。
  - enrich_geo 新增/补坐标 **121 条**；定位率 **62.4% → 84.0%**（350 → 471 / 561）。
- **② 尾端8章重抽（host-only，仅准备）**：写 `.dump/_reextract_tail8.md`（就绪检查 + 精确命令 + 收尾三步），沙箱不可运行，待用户在装有 OmniRoute 主机执行 `python src/extract_auto.py --dense --force p1-c17 p1-c22 p1-c28 p4-c11 p6-c12 p5-c2 p4-c4 p4-c15` → merge → generate → deploy。
- **部署总览**：HEAD 2f54b10（事件补年）→ 27ab7a5（geo 提升）。线上 `https://cochranek.github.io/ming/` 已含两项改进。
- **教训**：`manual_event_years.json` 初版误写 `#` 注释行导致 JSON 解析失败（JSON 不支持注释），改为纯 JSON + `_comment` 字符串键修正。新增数据文件须加入 `_deploy_reports.py` 的 FILES 清单才会上传（manual_event_years.json 已加）。

## 第七轮：用户放弃尾端8章重抽（"OmniRoute别管这个了，忘掉它"）
- 用户明确不再使用 OmniRoute 网关，尾端8章重抽（依赖本机 localhost:20128 LLM 网关）随之放弃，**不再作为待办/未完成项**。
- 已更新 MEMORY.md ② 状态为"已放弃，非待办"；`.dump/_reextract_tail8.md` 保留但标记过时，不再主动推进。
- **项目当前无 outstanding 任务**：事件补年(35→6)、地点定位率(62.4%→84%)、HTML 修复、report_p1 删除均已部署；尾端8章重抽已废弃。所有可交付项均已完成并上线 `https://cochranek.github.io/ming/`。

## 第八轮：待办清单彻底清零确认（用户："现在呢实现全部todo了吗"）
- 实测 data.json 终态：characters 1235 / locations 561（located 471，84.0%）/ events 1106（year_source=记忆 29 条）/ relations 2331 / timeline 1106 / reigns 17 / lifespans 72 / voyages 3 / meta 4。chapters.json 168 条目（156 章抽取完整）。
- TaskList 原残留 3 个 pending（#14 第陆部抽取 / #25 最终 merge 汇总 / #29 角色卡分级折叠）——均为早期抽取阶段遗留的陈旧项，数据已证明完成（p6 已抽、merge 已跑、charMinor 折叠开关代码已存在于 generate_report.py 第 3 行命中）。已标记 completed。
- 现 TaskList = 50 completed / 0 pending（#26 尾端8章重抽 已按用户"忘掉它"指令 deleted）。
- **结论：所有真实 todo 均已完成并部署，项目零未完项。** 仅余两个用户可选、纯本地的锦上添花优化（非待办）：① 全图力导向动画空闲后停 rAF 省 CPU；② 核对剩余 ~90 个未定位漠北军镇/泛称地点名。

## 第九轮：用户确认"要"——执行两项锦上添花（HEAD b991d2e）
- **① 全图力导向空闲停 rAF（省 CPU）**：`generate_report.py` 内联 JS `fullStep()` 原把 `alpha` 用 `floor=0.05` 兜底，导致 `alpha` 永不为 0、`fullLoop()` 常驻 rAF（"微抖动"刻意效果但常驻 CPU）；且 `fullLoop` 仅在 `reduced` 时停。`fullStep` 去掉 floor（`if(S.alpha<0)S.alpha=0`），`fullLoop` 改为 `if(fullSim.alpha<0.02&&dragIdx<0)` 即停 rAF（取消并置 raf=null）。效果：模拟自然衰减到 α<0.02（约空闲数秒）后彻底停机；拖拽节点（pointerdown 已 bump α+startFullSim）重新唤醒，缩放/平移/悬浮/点击均直接 drawFull 实时刷新且无需空转。reduced-motion 行为不变。
- **② 剩余未定位地点核验补全**：实测 data.json 90 个未定位（`ancient` 字段）→ 提取全量清单，按「高置信可考 / 模糊漠北远征故地 / 征安南·壬辰海外战场」分类，向 `enrich_geo.py` GAZ 新增 **49 条**（全部标"坐标来自内置地名词典（待核验）"，与源透明度纪律一致；先 `grep` 全名确认无重复 key，避免第六轮前"重复 key 静默覆盖"bug）。
  - 内地府州县卫所 32 条（延平/临江府/武进/蒲州/怀柔/山东东牟/狄道/河北容城/邵武/江西贵溪/怀安/天城/应州/八宝山/大峪山/甘孜松潘/西双版纳/湖北石首/化州/五通庙[祠堂]/大通桥/白羊口/龙门/宣平/滦城/平虏/猫儿庄/西平堡/中前所/前屯/右屯/车厢峡）。
  - 漠北远征故地 13 条（全宁/大兴州/东胜州/岭北[哈拉和林]/阿鲁浑河[鄂尔浑河]/称海/亦集乃路[黑水城]/胪朐河[克鲁伦河]/阔滦海子[呼伦湖]/忽兰忽失温/榆木川/兀良哈/达兰纳木尔河[土拉河]）。
  - 征安南·壬辰海外 4 条（多邦/东都[河内]/西都[清化]/马山馆）。
  - 重跑 `enrich_geo.py`（新增/补坐标 49）→ `merge.py`（已标坐标 471→520）→ `generate_report.py --scope full`。**定位率 84.0% → 92.7%**（471→520 / 561）。
- **残留 41 个未定位（明确列为"待专门核验"，不强行补）**：多为永乐北伐极 obscure 战场（柳河川/口温/扫林山/别笃山口/经江口/南湖口/松花河/百眼井/彻彻儿山/兀良哈秃城/娄桑/滑口/大伯颜山/回曲津/康哈里海/九龙口/沙珲原/齐拉尔河/翠微岗/黑松林/也失八秃儿/定羌驿/聚落堡/威武/笊篱山）+ 同名异地歧义（江口/白鹤/南安/龙山/宝山/猫岛/竹岛/六堡/杞店/五里寨/镇江堡/乳峰山）+ 非真实地点（暖阁=室内/车迟国=西游记典故/登莱=区域泛称）。这 41 条属"无法高置信定位"，留作后续专门史地核验清单。
- **部署**（HEAD b991d2e）：index.html 3596KB + data.json 1709KB + geo_annotations 135KB + src 三件套 + manual 两件套 + README；`_deploy_reports.py` 的 commit message 同步更新为本轮内容。
- **终态**：地点 561（已定位 520，92.7%）/ 事件 1106（年份待考 7）/ 人物 1235 / 关系 2331。全图力导向空闲零 CPU + 圣地巡礼坐标覆盖 92.7%。

## 第十轮：地点卡"无详细提及"反馈 → 同章上下文事件可浏览（HEAD fc677532e）
- **用户反馈**：地点详情常显示「书中直接关联事件（0）…同章另有 N 件事件…书中涉及人物 无（或未标注）」，很多地方没详细提及。
- **根因诊断（非 bug，属源诚实稀疏）**：`directEvents` 在 `generate_report.py` 的地点构建器（旧 line 607）用 `ancient in event.location or ancient in event.name` 子串匹配。实测：561 地点中 **295 个（53%）本就有直接事件**；其余 266 个是其 ancient 名确实不是同章任何事件的落点（仅被提及），故 0 直接事件。旧代码仅把"同章非直接事件"显示为一句灰色计数，无内容。
- **改动（纯 UI/数据派生，不伪造关联）**：
  1. 新增 `locationDetailHTML(x, evtAttr)` 共享函数，供地图 dock（`showLocation`）与卡片"详情"弹窗（`renderLocations` 的 `data-location-id` 处理）复用。
  2. 地点构建器新增计算 `relatedEvents`（同章非直接事件名，去重 cap 20）与 `relatedPeople`（这些事件的参与者，cap 20）；`is_direct` 改为**按 "/"、"、"、"·"、"," 分词后精确匹配 ancient∪mentioned_as 别名**（比子串更准，去掉"短名误命中长地名"的假阳性，直接事件 299→295 但质量更高）。
  3. 详情改为：直接关联事件（可点）→ **同章上下文事件（N）**：列出同章事件名为可点按钮，标注"书中同章提及，非本地点直接落点（可作圣地巡礼上下文）"；**书中涉及人物**在直接事件无参与者时，回退显示"来自同章上下文事件"的人物，解决"无（或未标注）"。
- **效果**：重建后模拟确认 **561/561 地点均有同章上下文事件可浏览**（如 湖口→鄱阳湖之战/龙湾之战/洪都守卫战…）；295 地点有真正直接事件。卡片不再"空"。
- **纪律**：上下文事件明确标注"非本地点直接落点"，未把同章事件伪造为地点直接落点，保持源透明度。
- **部署**（HEAD fc677532e）：index.html 3884KB + src/generate_report.py 134KB（含新函数）；`node --check` 内联 JS 语法通过。

## 第九轮（15:55 用户"类似的，你看看还有哪些地方不完善"）

**系统审计（纯数据诊断，非凭感觉）发现：**
- 人物卡 1235 中 **282 张"涉及事件：无"**（占 23%）；全部能补"同章上下文事件"——与地点改造完全对等。
- 关系网 2331 条源透明度正常（`chapter="curated"/"推导"`，非 `source` 字段，早期误读为"全是?"已澄清）；孤立 539/1235（44%，多为次要人物，设计如此）。
- 事件 1106：无地点 25、无年份 7（谜案保留）、无参与者 0；引用约 40 个真实地名不在 geo 库（古勒山/建州/吉林崖/盛京/应天/河间府/河间府…）→ 事件"相关地点"不可点（**待下一轮补 GAZ**）。
- 30 个事件参与者无人物卡（多为群体名词：倭寇/锦衣卫/东林党/后金/六部…），非真实个体缺口，跳过。

**已修（与地点改造对等）：人物卡"同章上下文事件"**
1. `build_scope` 新增 `events_by_chapter` + 每人物 `contextEvents`（同章非参与者事件名，去重）。
2. 人物卡翻面：无直接事件时显示 `同章提及` 事件 + 灰标注。
3. 人物详情弹窗 + 年谱人物详情：新增「涉及事件（N）」可点 + 「同章上下文事件（N）」可点列表，标注"书中同章提及，非本人物直接参与（可作关联线索）"；新增 `bindEventNameClicks()` 全局绑定 `data-event-name` → `showEvent`。
4. 修 KeyError：`events_by_chapter` 构建用 `event.get("chapters",[])`（个别事件缺键）。
5. `node --check` 内联 JS 语法通过。
- **效果**：模拟确认 282 张原空卡现均有同章上下文事件可浏览（廖永忠 17 件、赵普胜 15 件…）。
- **部署**（HEAD 7e319ffaa892d3d37c2a724ddc965cbb4b20e76e）：index.html + src/generate_report.py 136KB；线上 `https://cochranek.github.io/ming/`。

## 待办（用户可选，非本次范围）
- 第十轮候选：把事件引用的 ~40 个真实地名补入 `enrich_geo.py` GAZ（区分别名/子地点：平壤西城⊂平壤、王京=开城；新增古勒山/建州/吉林崖/盛京/应天/河间府/诏狱/洛水/黄河渡口等），让事件"相关地点"可点可绘。


## 第十一轮：多 Agent 交叉审计 + 修复（已部署 HEAD e7a0c12）
用户要求「严格找不同的 agent 检查」。派出 3 个不同视角 agent（数据质量 / 前端交互 / 明史准确性）只读审计，交叉验证后由我统一实施。

### 审计发现并已修复

**P0 崩溃级**
1. **20 条地点经纬度颠倒**（`lat=136.47` 物理不可能，全为日/朝/越条目）。根因：上轮补 `enrich_geo.py` GAZ 时写成了 `(lat,lng)` 而非约定的 `(lng,lat)`。已逐行正则修正 GAZ 19 条 + `geo_annotations.json` 20 条 + `data.json` 20 条，占城坐标统一为 (109.22, 13.77)。
2. **16 条地点未走归一化导致前端 TypeError**：`event_places` 注入发生在归一化循环「之后」，新地点缺 `id/modern/mentionedAs/trace/region/directEvents`，`locationCard`/`locationDetailHTML` 会崩。改法：把注入提到 `for location in raw_locations` **之前**，让新地点走同一套构造；前端再加 `||[]` 兜底。
3. **CSS `@media(max-width:760px)` 缺一个 `}`**：导致 967 行之后的**全部 CSS** 被嵌套进媒体查询（深度=1），桌面端大面积失效（力导图 tip 常驻挡点击、详情弹窗不滚动、`.src-tag` 失效）。补 `}` 后 CSS 深度归 0。
4. **打印/扑克牌导出整页空白**：`#printArea` 在 `.panel` 内，而 `@media print` 有 `.panel{display:none!important}`——祖先隐藏时后代无法重显。改为保留 `#characters .panel`、只隐藏其内部控件。

**P1 前端功能**
5. 力导图：多次渲染叠加多个 rAF（`stopFullSim()` 前置）；`_fullBound` 布尔守卫在 innerHTML 重建后失效 → 改用 `canvas.dataset.fgBound` + **AbortController**（重绑定自动注销旧 window 监听）；补 `pointerleave` 收起提示框。
6. 地点 chip 在非地图视图点击"无反应"（内容写入隐藏的 `#locDock`）→ `showLocation` 判断 `state.view!=='map'` 时改走 `openDetail` 弹窗。
7. `bindEventNameClicks` 扩展支持 `data-loc-event`；地点详情弹窗补调用。
8. 区域下拉出现空选项（`undefined` 混入）→ `.filter(Boolean)`；「按章节」模式下搜索/区域是死控件 → 该模式隐藏工具栏。
9. 热力图 3744 个无行为 `<button>` 污染 Tab 序 → 改 `<span role="img">`。
10. 「重置」漏清 `state.charMinor`。

**P2/P3 数据 + 史实（全部经 `data/manual_corrections.json` 落地，重跑 merge 不丢）**
- 新增 **`data/manual_corrections.json`** 勘误管道：`event_years` / `character_merges` / `character_alias_remove` / `character_faction` / `character_role` / `location_fixes` / `relation_fixes(flip|drop)`，在 merge.py 关系构建**之前**应用，并写入 `meta.cleaning.corrections`。
- 事件年份修正 10 条（鸣梁 1598→1597、玉浦 1591→1592、张献忠襄阳 1640→1641、杨嗣昌 1640→1641、赐名朝鲜 1388→1392、一条鞭法 1573→1581、戚继光袭职 1538→1544、关锦防线/孙承宗弹劾/魏忠贤戮尸 年号与公元年自相矛盾）。
- `manual_event_years.json` 修正 5 条（豹房 1517→1507、唐伯虎装疯 1499→1514、李默 1550→1556、阿鲁台 1408→1413、王守仁格竹 1490→1492），不确定项标 `approx`。
- 人物合并 6 组（崇祯皇帝→朱由检 章数 4→14、韩旷→韩爌、张维迎→张维贤、徐宏祖→徐霞客、李贵妃→李太后、王宫女→王恭妃）。
- 误挂别名清除：汪直（西厂太监）的 `五峰/老船主/汪老板/王直`（实为海盗王直）、尚之信的 `尚可喜`（实为其父，二人非同一人）。
- 势力/身份勘误：魏忠贤→明朝·阉党、王振→明朝·宦官（"阉党"专指天启魏集团，标王振属时代错置）、方孝孺/齐泰/黄子澄→建文朝廷、毛文龙（原书误作杀佟养性，实为佟养真）、唐寅（非程敏政女婿）。
- **亲属关系统一为「长辈→晚辈」**：翻转 17 条（父子/母子/祖孙）；剔除 8 条矛盾重复与跨代错配（`严世蕃→严嵩` 与反向重复；「朱祁镇遗愿废殉葬」把追述的朱元璋/朱棣列成同事件参与者产生 4 条跨代关系）。
- `event_places.json` 的 `王京→开城` 是错的（王京=朝鲜王京汉城=今首尔，开城是高丽旧都）→ 改指向已存在的 `王京（首尔）`；釜山今址"今朝鲜"→"今韩国"。

### 终态
地点 577（已定位 536）/ 人物 1229 / 事件 1106 / 关系 2322（悬空端点 74→46，余为地点/机构型，merge.py 已归类为"地点关联"）。CSS 深度 0、`node --check` 通过。

### 遗留（未修，需人工判断）
- `父子 张瑾→张軏` 方向存疑（史料未确证，保持原样）。
- 关系端点 46 个为地点/机构型（东林党/后金/鄱阳湖/土木堡…），目前归为"地点关联"类别，可考虑建独立"组织/地点"实体。
- `reigns` 表填的是在位年而非年号元年（建文 1398 vs 1399，差 1 年），字段名为 `era`，下游按年号渲染会整体错位一年。
- 地点 `mentioned_as` 跨卡串味 83 类（甘肃卡含兰州/定西，山西卡含大同），部分为有意扇出，需人工区分。
- `renderPrint` 源模板末尾有游离反引号，靠 `document.replace` hack 兜底（L1223），后续改动该函数前应先清理。
