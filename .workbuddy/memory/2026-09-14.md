# 2026-09-14 工作日志

## 发布：洞察实体联动 + 书内语录扩面（00:10，用户「同步」）

承接 `2026-09-13.md` 第六轮的两项内容层成果，一次性发布上线。

- **同步** `_sync_docs.py`：上传 **14 个变化文件**，跳过 62 个 → commit **`9d7e27c4`**。
  - 关键新增：`src/core/insight_link.py`（洞察联动索引，**已预先补进 FILES 清单**，否则 CI 会报缺文件）、`web/js/app.js`、`web/css/app.css`、`sw.js`、`src/generate_report.py`、`data/character_quotes.json`、`tests/test_data_invariants.py`、`.dump/_browser_check.py`、`.dump/_sync_docs.py`、3 个记忆/skill 文档 + `index.html`（5,459,558 B）。
  - **未跑 `_deploy_index_now.py`**：`_sync_docs.py` 是超集（index.html/sw.js/README.md 都在其 FILES 里），重复跑只会多一个空提交。
- **发布后核验**：
  - `_diff_remote.py` → **完全一致 ✅**（未传 0 / 过时 0 / 仅线上 1 白名单 `chapters_index.json`）。
  - 合规巡检：77 个 blob，**敏感路径 0 命中**。
  - 线上 `index.html`：`insightIndex` 3 处、`延安府（朝鲜）`/`龙山（朝鲜）` 各 7 处 —— 第六轮数据与本轮联动**均已在线**。
  - `sw.js`：首次带 `?v=8` 拉到 **1987 B 的陈旧 v7 副本**（Pages CDN 边缘缓存 `max-age=600`）；换时间戳 cache-buster 后 raw 与 Pages **均返回 v8**（2,047 字符）。**教训已记**：校验线上文件不要用固定查询串，可能被 CDN 按该 URL 缓存住，用时间戳或换 raw 域名。
  - Actions：`9d7e27c4` 的 **CI** 与 **pages build and deployment** 全部 **success**。
- **上线终态**：人物 1231 / 语录 **66 人** / 地点 583（已标坐标 553）/ 事件 1106（待考 2）/ 关系 2324 · 洞察 19 节全实体可点击 · **`CACHE ming-report-v8`**。

## 内容层待办现状（本轮后）

- 已完成：同名异地拆分、事件补年（7→2）、地点定位（41→30）、**语录扩面（13→66）**、**洞察实体联动**。
- **仅剩**：数据清洁（低）：端点别名归一、`mentioned_as` 串味、张瑾→张軏方向。
