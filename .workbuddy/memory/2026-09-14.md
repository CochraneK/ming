# 2026-09-14 工作日志

## 发布：洞察实体联动 + 书内语录扩面（00:10，用户「同步」）

承接 `2026-09-13.md` 第六轮的两项内容层成果，一次性发布上线。

- **同步** `_sync_docs.py`：上传 **14 个变化文件**，跳过 62 个 → commit **`9d7e27c4`**。
  - 关键新增：`src/core/insight_link.py`（洞察联动索引，**已预先补进 FILES 清单**，否则 CI 会报缺文件）、`web/js/app.js`、`web/css/app.css`、`sw.js`、`src/generate_report.py`、`data/character_quotes.json`、`tests/test_data_invariants.py`、`.dump/_browser_check.py`、`.dump/_sync_docs.py`、3 个记忆/skill 文档 + `index.html`（5,459,558 B）。
  - **未跑 `_deploy_index_now.py`**：`_sync_docs.py` 是超集（index.html/sw.js/README.md 都在其 FILES 里），重复跑只会多一个空提交。
- **发布后核验**：
  - `_diff_remote.py` → **完全一致 ✅**（未传 0 / 过时 0 / 仅线上 1 白名单 `chapters_index.json`）。
  - 合规巡检：77 个 blob，**敏感路径 0 命中**。
  - 线上 `index.html`：`insightIndex` 3 处、`延安府（朝鲜）`/`龙山（朝鲜）` 各 7 处 —— 第六轮数据与本轮联动**均已在线**。
  - `sw.js`：首次带 `?v=8` 拉到 **1987 B 的陈旧 v7 副本**（Pages CDN 边缘缓存 `max-age=600`）；换时间戳 cache-buster 后 raw 与 Pages **均返回 v8**（2,047 字符）。**教训已记**：校验线上文件不要用固定查询串，可能被 CDN 按该 URL 缓存住，用时间戳或换 raw 域名。
  - Actions：`9d7e27c4` 的 **CI** 与 **pages build and deployment** 全部 **success**。
- **上线终态**：人物 1231 / 语录 **66 人** / 地点 583（已标坐标 553）/ 事件 1106（待考 2）/ 关系 2324 · 洞察 19 节全实体可点击 · **`CACHE ming-report-v8`**。

## 内容层待办现状（本轮后）

- 已完成：同名异地拆分、事件补年（7→2）、地点定位（41→30）、**语录扩面（13→66）**、**洞察实体联动**。
- **仅剩**：数据清洁（低）：端点别名归一、`mentioned_as` 串味、张瑾→张軏方向。

## 自查轮（用户：「自我检查是否还有bug和改善之处」）

**查出 5 个真实缺陷（全部自查发现，非用户报告），已全部修复并加回归闸。**

1. **审计 R-GEO-03 同名异地 12 处误报 → 真冲突只有 0 处。**
   根因：`geo_annotations.json` 里 95 条占位条目（`{"status":"抽取待补"}`，**没有 lat 键**），
   `float(x.get("lat", 0))` 被算成 `(0,0)`，与真坐标凑成「两个候选」。
   修：`audit_final.py` 先滤掉无坐标/零坐标条目；同时清掉 11 条「已有正式标注仍留占位」的重复条目，
   把 `延安府` 的朝鲜条目改键为 `延安府（朝鲜）`（原记 126.55 疑为 126°05′E 之误，订正 126.0833/37.9167）。
   `geo_annotations` 668 → 657；审计 **同名异地 0**。
2. **审计 R-REL-00 是恒为 0 的死表达式**：`"person" not in (k_from, k_to, "person")` 条件永假，
   与 R-GRAPH-00「排除 72 条」自相矛盾。修成 `_kinds(r) != ("person","person")` → **72 == 72 一致**。
   （中间还踩了一次：写成 `"person" not in _kinds(r)` 会误判成「两端都非人物」，得 3。）
3. **前端：详情弹窗里点「相关洞察」chip 像没反应。** `<dialog>` 是 top-layer，
   `gotoInsight()` 只滚动背景正文，弹窗仍盖在上面。修：goto 前先 `close()` 弹窗。
4. **前端：洞察链接找不到目标时静默 return。** 改为 `failBar(...,{level:'warn'})` 可见提示（3 处）。
5. **`mentionedAs` 混入说明片段却挂「别称」标签。** 1377 条里 938 条是
   「洪承畴籍贯」「袁崇焕驻守，高第撤防时唯一不撤之城」这类说明，地点卡上出现
   「别称：熊廷弼不守、努尔哈赤退兵错过之关键据点…」。新增 `src/core/place_mentions.py` 构建期分级：
   `altNames` 142 条（真别称）+ `mentionContext` 1235 条（说明），`mentionedAs` **原样保留**（深链/搜索依赖它）；
   前端拆成「别称」「书中提及（N）」两块。规则 = 正向形状约束（2~8 字、无标点、以地名尾字收尾）
   + 负向排除（描述词 / 其他实体名 / 「今」开头）。**判不准一律归 context——只换标题，不丢数据。**

**第 6 项：真机自检脚本自身的假警报。** `_browser_check.py` 里 Chrome `--dump-dom` 偶发返回 0 字符
（同一场景第一次挂、重跑就过），已改为空结果**最多重试 3 次**（间隔 0.6s）。
不修的话真回归会被噪声淹没。

**加固（防回归）**：测试 46 → **55** 例（`altNames` 形状闸 / mention 划分无损闸 / 地点+事件索引互逆 /
alias 值必为规范名 / geo 无重复占位 / geo 无坐标冲突 / excludedNonPerson 单一口径）；
浏览器自检 13 → **14** 场景（新增「地点卡 · 别称与书中提及分级（宁远）」）；
`_sync_docs.py` FILES 补 `src/core/place_mentions.py`。

**终态核验**：`build.py --check` ERROR 0/WARNING 1/INFO 5；`audit_final.py` **INFO 17 · WARNING 1 · ERROR 0**；
`run_tests.py` **55/55**；`_browser_check.py` **14/14**；单文件 **5.21 MB → 5.27 MB**（+60 KB，分级字段）；
`sw.js` **v8 → v9**（前端改动，待同步）。**仅剩 WARNING 是有意保留的 2 件年份待考事件。**

**新增产出**：`report/Ming_自查报告_2026-09-14.md`（六项缺陷的根因/改动/回归闸 + 三类系统性风险），
已补进 `_sync_docs.py` FILES（现 79 条，缺失 0）。`.dump/` 一次性自查脚本已清理，只留常驻工具。

## 自查轮发布（用户：同步）

- **三次提交**（都走 `_sync_docs.py`，它是超集，无需再跑 `_deploy_index_now.py`）：
  - `2d447a8c` —— 15 个变化文件（`index.html` 5,523,279 B / `sw.js` / `src/audit_final.py` /
    `src/core/place_mentions.py`（新）/ `src/generate_report.py` / `data/geo_annotations.json` /
    `web/js/app.js` / `tests/test_core.py` / `tests/test_data_invariants.py` /
    `.dump/_sync_docs.py` / `.dump/_browser_check.py` / `report/Ming_自查报告_2026-09-14.md`（新）+
    记忆与 skill）。跳过 64 个未变化文件。
  - `ff9e6184` —— `sw.js` 补 v9 变更日志行（原文件只有 v5~v8 的记录，缺 v9）。
  - `fe383c1d` —— 新增 `.dump/_post_publish_check.py`（发布后体检，见下）。
- **发布后体检（新脚本一次跑完三件事）**：合规巡检 **81 个 blob / 敏感路径 0 命中 ✅**；
  线上产物**逐字节核对**（`index.html` 5,523,279 B、`sw.js` 3,147 B，sha1 与本地完全一致 ✅，
  `CACHE=v9`）；Actions 结论。
- `_diff_remote.py` → **完全一致 ✅**（未传 0 / 过时 0 / 仅线上 1 白名单 `chapters_index.json`）。
- `2d447a8c` 与 `ff9e6184` 的 CI 与 pages 部署均 **success**（`fe383c1d` 查询时仍在跑）。

## 新增常驻工具：`.dump/_post_publish_check.py`

把「发布后必须核验的三件事」固化成一条命令，避免每次现拼：

1. **合规巡检**——查的是**线上仓库文件树**（本地 `.gitignore` 挡不住 API 上传）；
2. **线上产物逐字节核对**——必须用**时间戳 cache-buster** 绕 Pages 边缘缓存
   （`max-age=600`；**固定查询串会被按那个 URL 缓存住**，曾拉到上一版 sw.js）；
   顺带解出 `sw.js` 里的 `CACHE` 名，确认 bump 真的生效；
3. **CI 结论**——`gh` 需清掉宿主代理环境变量（沙箱 loopback 连不上 `127.0.0.1:7897`）。

退出码：合规与产物都过才 0，可直接串进脚本。
